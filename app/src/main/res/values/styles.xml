<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorEbonyClay</item>
        <item name="colorPrimaryDark">@color/colorEbonyClay</item>
        <item name="colorAccent">@color/colorEbonyClay</item>
        <item name="android:windowBackground">@color/colorBlackRock</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>